#include "bits/stdc++.h"
using namespace std;

/**
 * Count sub array
 * Given a positive integer n, you are allowed to use an 
 * unlimited number of natural numbers from 1 to n. 
 * Ask how many ways are there to choose a sequence whose elements sum to n.
 *
 * input:
 *      1<=N<=10^12
 *
 * output: print the number of way choose a sequence whose elements sum to n.
 *
 * @example
 *      input:
 *          6
 *      output:
 *          32
 */

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr); cout.tie(nullptr);

    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
    
    return 0;
}















